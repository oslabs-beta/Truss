version: "1"

layers:
  api:
    - "src/api"
  service:
    - "src/service"
  db:
    - "src/db"

rules:
  - name: no-api-to-db
    from: api
    disallow: [db]
    message: API layer must not depend on DB layer.
